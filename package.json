{
  "name": "gaming-chat",
  "version": "1.0.0",
  "description": "Real-time multiplayer gaming chat platform",
  "scripts": {
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "build": "cd client && npm install && npm run build",
    "start": "cd server && node src/server.js",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "render-build": "cd client && npm install --include=dev && npm run build && cd ../server && npm install"
  },
  "keywords": [
    "gaming",
    "chat",
    "multiplayer",
    "socket.io"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
